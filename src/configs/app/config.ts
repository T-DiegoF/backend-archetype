export enum ConfigurationDB {
  DB_PORT = 'DB_PORT',
  DB_HOST = 'DB_HOST',
  DB_USERNAME = 'DB_USERNAME',
  DB_PASSWORD = 'DB_PASSWORD',
  DB_DATABASE = 'DB_DATABASE',
}

export enum ConfigurationAPP {
  PORT = 'PORT',
  JWT_SECRET = 'JWT_SECRET',
}
